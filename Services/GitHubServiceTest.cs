using BenjaminFriske.Portal.Models;
using BenjaminFriske.Portal.Services;
using BenjaminFriske.Portal.Tests.Fakes.Models;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace BenjaminFriske.Portal.Tests.Services;


[TestClass]
public class GitHubServiceTest {

    [TestMethod]
    public void GetUsers_PassingUserName_ValidUserModel() {
       /*  // Arrange
        GithubService service = new GithubService(new HttpRequestService());
        UserModel mock = UserModelFake.CreateUser();
        
        // Act
        var result = service.GetUser(mock.Login);

        // Assert
        Assert.AreSame(result, mock); */
    }
}